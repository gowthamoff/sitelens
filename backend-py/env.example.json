{
  "_comment": "Copy to env.json (gitignored) and use with: sam local start-api --env-vars env.json. Points the Lambda at any reachable PostGIS (the new RDS, or a local PostGIS docker).",
  "ApiFunction": {
    "RDS_HOST": "your-rds-endpoint.rds.amazonaws.com",
    "RDS_PORT": "5432",
    "RDS_DB": "osm_tn",
    "RDS_USER": "postgres",
    "RDS_PASS": "your-db-password",
    "RDS_SSLMODE": "require",
    "JWT_SECRET": "local-dev-secret-change-me-1234567890",
    "JWT_EXPIRES_IN": "7d",
    "GOOGLE_PLACES_KEY": "",
    "ALLOWED_ORIGIN": "*",
    "NODE_ENV": "development"
  }
}
